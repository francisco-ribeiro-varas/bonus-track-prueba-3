tabla:
me ayudas a hacer la tabla en analisis.md
fue util para llenar la tabla con informacion 

etapa 1:
Asísteme en importar los arrays en app.jsx y revisa en la consola que se carga (console.log).
fue util para mostrar los array en la consola

etapa 2:
En MascotaCard.jsx ubicado en la carpeta components, el componente recibe por props: nombre, raza, 
edad, especie, descripción, características, y debe presentar la información de manera visual y organizada, 
además de aplicar un estilo diferenciado según la especie (un color/etiqueta diferente para perro, gato, u otro).

resultó útil para exhibir las mascotas en páginas y con presentaciones distintas para cada especie, 
mostrando información ordenada.

etapa 3:

En ListaMascotas.jsx ubicada en componentes, debe aceptar el array de mascotas a través de props, 
recorrer el array utilizando .map() y renderizar un MascotaCard para cada mascota, asignando adecuadamente la prop 
key usando el id de cada elemento (no emplees el índice del array), y en app.jsx se debe renderizar ListaMascotas pasándole 
los datos importados.

Fue beneficioso para desarrollar el código de manera modular y escalable.

etrapa 4:

en FiltoEspecie.jsx en la carpeta components incluye un selector para filtrar por especie: todas, perros, 
gatos, otros, y en app.jsx utiliza usestate para almacenar el filtro seleccionado y un campo de búsqueda por nombre; 
aplica ambos filtros sobre el array antes de enviarlo a ListaMascotas y emplea renderizado condicional para mostrar el 
mensaje "no hay mascotas disponibles" cuando el resultado esté vacío.

Resultó ser útil para integrar criterios de búsqueda avanzados.

etapa 5: 
destaca de manera visual los elementos con adopcionUrgente: true (por ejemplo, un borde característico) y 
añade un contador que indique cuántas mascotas requieren adopción urgente, además de asegurarte de que toda 
la aplicación funcione sin errores ni advertencias en la consola.

Resultó beneficioso para enfocar la atención del usuario: El diseño visual prominente (como el contorno) 
orienta la mirada instantáneamente hacia las situaciones más críticas (adopcionUrgente: true) para que no se ignoren.

